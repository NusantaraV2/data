# NusantaraV2 — Data

Encrypted data assets for the NusantaraV2 companion app.

```
catalog/   hero & content catalog (encrypted)
tools/     offline companion datasets (encrypted)
icons/     ability icons
```

All `.txt` files are encrypted binary payloads consumed by the app.
